package com.geneforge.android.utils

import android.content.Context

class JwtTokenManager(private val context: Context) {
    fun saveToken(token: String) {
        // TODO: Save to SharedPreferences
    }
}